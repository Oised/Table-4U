{
  "name": "publicserver",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "generate": "npx prisma generate && npx prisma generate --schema=prisma/schema_private.prisma",
    "postinstall": "npm run generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
